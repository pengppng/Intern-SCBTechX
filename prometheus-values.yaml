grafana:
  enabled: true
  adminPassword: admin123
  service:
    type: ClusterIP
  ingress:
    enabled: false

prometheus:
  enabled: true
  service:
    type: ClusterIP
  ingress:
    enabled: false

alertmanager:
  enabled: true
  service:
    type: ClusterIP

nodeExporter:
  enabled: true

prometheusOperator:
  enabled: true
  admissionWebhooks:
    enabled: false

kubeStateMetrics:
  enabled: true

defaultRules:
  create: true
